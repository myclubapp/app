export interface Training {
  title: string;
  description: string;
  location: string;
  timeFrom: string;
  timeTo: string;
  weekday: string;
  startDate: string;
  repeat: string;
  clubId: string;
  teamId: string;
}
